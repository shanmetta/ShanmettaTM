<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shanmetta_tm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dataUser"></activity>
        <activity android:name=".hotelListAdapter" />
        <activity android:name=".dataHotel" />
        <activity android:name=".isimybooking" />
        <activity android:name=".checkin" />
        <activity android:name=".chekin" />
        <activity android:name=".detailhotel" />
        <activity android:name=".toolbar" />
        <activity android:name=".mybooking" />
        <activity android:name=".isirow" />
        <activity android:name=".Homepages" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>